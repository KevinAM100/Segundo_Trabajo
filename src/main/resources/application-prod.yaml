server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/usuarios_prod_db
    username: postgres
    password: 4530873kopq
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    locations: classpath:db/migration,classpath:prod/db/migration