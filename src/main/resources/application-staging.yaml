server:
  port: 8089

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/usuarios_staging_db
    username: postgres
    password: 4530873kopq
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    locations: classpath:db/migration,classpath:staging/db/migration